{% extends 'base.j2' %}
{% block content %}
    <p class="text-lg font-semibold">
        Log in, friend.
    </p>
    <p class="w-1/3 m-12 hidden">
        No passwords here. Just provide a username and you'll be in.
        If this is your first time here, you'll be added.
        Your username will be public.
    </p>

    <form class="p-8 rounded bg-gray-400 w-1/4 flex flex-col gap-6" method="post" autocomplete="off">
        <label class="">
            <p class="text-xs text-gray-50">Username</p>
            <input type="text" placeholder="Username" name="username" class="p-2 rounded"/>
        </label>

        <button type="submit" class="py-2 px-5 rounded-lg border border-gray-800 hover:bg-slate-500 bg-slate-300 w-48">
            Log In
        </button>
    </form>
{% endblock %}
